# Production overrides
server:
  port: ${PORT:8080}

logging:
  level:
    root: WARN
    com.example.sample_starter: INFO

# Example placeholders for when you add a datasource dependency:
# spring:
#   datasource:
#     url: ${DB_URL}
#     username: ${DB_USERNAME}
#     password: ${DB_PASSWORD}
