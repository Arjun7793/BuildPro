package com.example.buildpro.repository;

import com.example.buildpro.entity.SiteSectionSettings;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SiteSectionSettingsRepository extends JpaRepository<SiteSectionSettings, Long> {
}
